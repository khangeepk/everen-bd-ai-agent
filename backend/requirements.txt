# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.27
gunicorn>=22.0
pydantic>=2.6
pydantic-settings>=2.2

# --- Database ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pgvector>=0.2.5

# --- Auth ---
pyjwt[crypto]>=2.8
httpx>=0.27

# --- AI / LLM ---
openai>=1.30

# --- Logging ---
python-json-logger>=2.0

# --- Error alerting (Phase E) ---
# Purely opt-in: sentry_sdk.init() is only called when SENTRY_DSN is set
# (app/main.py); with no DSN configured this dependency is inert.
sentry-sdk[fastapi]>=2.0

# --- Encryption at rest (PII columns) ---
cryptography>=42.0

# --- Background tasks ---
celery>=5.3
redis>=5.0

# --- Email ---
sendgrid>=6.11

# --- Testing ---
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
aiosqlite>=0.20
